# GPU 算力平台 — 系統架構規劃

## 核心原則

**不動 OpenNebula，在旁邊蓋新的。**

OpenNebula 整套（oned + FireEdge + Sunstone）完全不改，新開發的 GPU 算力平台作為獨立系統，透過 FireEdge API 與 OpenNebula 溝通。

---

## 完整架構圖

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│  ┌───────────────────────┐    ┌───────────────────────┐        │
│  │  GPU 算力平台（新開發）  │    │  Sunstone（原本的）    │        │
│  │  React 前端            │    │  管理員進階設定用      │        │
│  │  Port 3000             │    │  Port 2616            │        │
│  └───────────┬───────────┘    └───────────┬───────────┘        │
│              │                             │                    │
│              │ 所有 API                     │ 直接存取           │
│              ▼                             │                    │
│  ┌─────────────────────────────────────┐   │                    │
│  │  GPU Platform 微服務（新開發）        │   │                    │
│  │  Express.js                         │   │                    │
│  │  Port 4000                          │   │                    │
│  │                                     │   │                    │
│  │  ┌───────────────────────────────┐  │   │                    │
│  │  │ 自建業務 API                   │  │   │                    │
│  │  │                               │  │   │                    │
│  │  │ /api/applications/*  申請審核  │  │   │                    │
│  │  │ /api/schedules/*     行事曆   │  │   │                    │
│  │  │ /api/vip/*           VIP 插隊 │  │   │                    │
│  │  │ /api/audit/*         審計日誌  │  │   │                    │
│  │  │ /api/notifications/* Email通知 │  │   │                    │
│  │  │ /api/dashboard/*     GPU 看板  │  │   │                    │
│  │  └───────────────┬───────────────┘  │   │                    │
│  │                  │ 讀寫              │   │                    │
│  │  ┌───────────────▼───────────────┐  │   │                    │
│  │  │ GPU Platform DB               │  │   │                    │
│  │  │ PostgreSQL                    │  │   │                    │
│  │  │ Port 5432                     │  │   │                    │
│  │  └───────────────────────────────┘  │   │                    │
│  │                                     │   │                    │
│  │  ┌───────────────────────────────┐  │   │                    │
│  │  │ 代理轉發 API                   │  │   │                    │
│  │  │                               │  │   │                    │
│  │  │ /api/one/*  → 轉發到 FireEdge │──│───┘                    │
│  │  │ （VM/Template/Host/User 等）  │  │                        │
│  │  └───────────────────────────────┘  │                        │
│  └──────────────────┬──────────────────┘                        │
│                     │ HTTP 轉發                                  │
│  ┌──────────────────▼──────────────────────────────────────┐    │
│  │  FireEdge（完全不改）                                     │    │
│  │  Express.js + Socket.IO                                  │    │
│  │  Port 2616                                               │    │
│  │                                                          │    │
│  │  ├── 功能路由（83 個）  → 登入/Guacamole/上傳 等         │    │
│  │  ├── XML-RPC 代理（283 個）→ VM/Host/Template CRUD       │    │
│  │  └── WebSocket → Hook 事件 + Guacamole 遠端桌面          │    │
│  └──────────────────┬──────────────────────────────────────┘    │
│                     │ XML-RPC                                    │
│  ┌──────────────────▼──────────────────────────────────────┐    │
│  │  OpenNebula Daemon (oned)（完全不改）                      │    │
│  │  Port 2633                                               │    │
│  │                                                          │    │
│  │  ├── 排程器（Scheduler + USER_PRIORITY）                  │    │
│  │  ├── OpenNebula DB（SQLite 或 MySQL，oned 自己管）        │    │
│  │  └── 管理 KVM 虛擬化                                     │    │
│  └──────────────────┬──────────────────────────────────────┘    │
│                     │                                            │
│  ┌──────────────────▼──────────────────────────────────────┐    │
│  │  GPU Host（實體伺服器）                                    │    │
│  │  10.1.1.79                                               │    │
│  │                                                          │    │
│  │  ├── KVM 虛擬化引擎                                      │    │
│  │  ├── NVIDIA GPU（PCI Passthrough / vGPU / MIG）          │    │
│  │  ├── DCGM 監控 agent                                     │    │
│  │  └── VM 實例（學生的虛擬機）                              │    │
│  └──────────────────────────────────────────────────────────┘    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## Port 規劃

| 服務 | Port | 技術 | 說明 |
|------|------|------|------|
| GPU 算力平台前端 | 3000 | React | 新開發，學生和管理員使用 |
| GPU Platform 微服務 | 4000 | Express.js | 新開發，業務邏輯 + API 代理 |
| GPU Platform DB | 5432 | PostgreSQL | 新開發，存業務資料 |
| FireEdge | 2616 | Express.js | 不動，OpenNebula Web UI + API |
| oned | 2633 | C++ | 不動，OpenNebula 核心引擎 |
| Guacamole (guacd) | 4822 | C | 不動，遠端桌面 daemon |

---

## 資料庫規劃

### 原則：兩個 DB 各管各的

```
OpenNebula DB（不動）                GPU Platform DB（新開）
由 oned 管理                         由你的微服務管理
SQLite 或 MySQL                      PostgreSQL

存的東西：                            存的東西：
├── vm_pool（VM 資料）                ├── applications（申請單）
├── user_pool（使用者）               ├── application_reviews（審核紀錄）
├── host_pool（主機）                 ├── schedules（行事曆排程）
├── template_pool（範本）             ├── vip_preemptions（VIP 插隊紀錄）
├── image_pool（映像檔）              ├── audit_logs（操作審計日誌）
├── vnet_pool（虛擬網路）             ├── login_history（登入活動）
└── ...其他 OpenNebula 資源           ├── email_notifications（通知紀錄）
                                     └── gpu_metrics（GPU 監控快照）
```

### PostgreSQL 資料表設計

```sql
-- ============================================================
-- 申請審核
-- ============================================================

CREATE TABLE applications (
    id            SERIAL PRIMARY KEY,
    student_name  VARCHAR(100) NOT NULL,
    student_id    VARCHAR(50) NOT NULL,        -- 學號
    email         VARCHAR(255) NOT NULL,
    purpose       TEXT,                         -- 申請用途
    gpu_spec      VARCHAR(100),                 -- 需要的 GPU 規格
    status        VARCHAR(20) DEFAULT 'pending', -- pending / approved / rejected
    created_at    TIMESTAMPTZ DEFAULT NOW(),
    updated_at    TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE application_reviews (
    id              SERIAL PRIMARY KEY,
    application_id  INTEGER REFERENCES applications(id),
    reviewer_name   VARCHAR(100),               -- 審核者
    decision        VARCHAR(20) NOT NULL,        -- approved / rejected
    reason          TEXT,                         -- 審核原因
    one_user_id     INTEGER,                     -- 審核通過後建立的 OpenNebula User ID
    one_vm_id       INTEGER,                     -- 審核通過後建立的 VM ID
    created_at      TIMESTAMPTZ DEFAULT NOW()
);

-- ============================================================
-- 行事曆排程
-- ============================================================

CREATE TABLE schedules (
    id              SERIAL PRIMARY KEY,
    one_user_id     INTEGER NOT NULL,            -- OpenNebula User ID
    one_vm_id       INTEGER NOT NULL,            -- OpenNebula VM ID
    gpu_host_id     INTEGER,                     -- 指定的 GPU Host ID
    start_time      TIMESTAMPTZ NOT NULL,        -- 時段開始
    end_time        TIMESTAMPTZ NOT NULL,        -- 時段結束
    repeat_type     VARCHAR(20),                 -- once / daily / weekly
    repeat_days     VARCHAR(50),                 -- 週幾（例如 "1,3,5" = 週一三五）
    is_active       BOOLEAN DEFAULT TRUE,
    created_at      TIMESTAMPTZ DEFAULT NOW()
);

-- ============================================================
-- VIP 插隊
-- ============================================================

CREATE TABLE vip_preemptions (
    id                  SERIAL PRIMARY KEY,
    vip_user_id         INTEGER NOT NULL,        -- VIP 的 OpenNebula User ID
    vip_vm_id           INTEGER NOT NULL,         -- VIP 的 VM ID
    preempted_vm_id     INTEGER NOT NULL,         -- 被暫停的 VM ID
    preempted_user_id   INTEGER NOT NULL,         -- 被暫停的 User ID
    gpu_host_id         INTEGER,                  -- GPU Host ID
    action              VARCHAR(20) NOT NULL,      -- preempt / restore
    status              VARCHAR(20) DEFAULT 'active', -- active / restored
    preempted_at        TIMESTAMPTZ DEFAULT NOW(),
    restored_at         TIMESTAMPTZ
);

-- ============================================================
-- 操作審計日誌
-- ============================================================

CREATE TABLE audit_logs (
    id              SERIAL PRIMARY KEY,
    user_id         INTEGER,                     -- 操作者 OpenNebula User ID
    user_name       VARCHAR(100),
    action          VARCHAR(100) NOT NULL,        -- 例如 "vm.action.poweroff"
    target_type     VARCHAR(50),                  -- vm / user / template / host
    target_id       INTEGER,
    request_body    JSONB,                        -- API 請求內容
    response_status INTEGER,                      -- HTTP 回應碼
    ip_address      VARCHAR(45),
    created_at      TIMESTAMPTZ DEFAULT NOW()
);

-- ============================================================
-- 登入活動
-- ============================================================

CREATE TABLE login_history (
    id          SERIAL PRIMARY KEY,
    user_id     INTEGER NOT NULL,
    user_name   VARCHAR(100),
    ip_address  VARCHAR(45),
    user_agent  TEXT,                             -- 瀏覽器資訊
    action      VARCHAR(20) NOT NULL,             -- login / logout / login_failed
    created_at  TIMESTAMPTZ DEFAULT NOW()
);

-- ============================================================
-- Email 通知紀錄
-- ============================================================

CREATE TABLE email_notifications (
    id              SERIAL PRIMARY KEY,
    recipient_email VARCHAR(255) NOT NULL,
    subject         VARCHAR(500),
    template_type   VARCHAR(50),                  -- approval / rejection / vip_alert / schedule_reminder
    related_id      INTEGER,                      -- 關聯的申請單或排程 ID
    status          VARCHAR(20) DEFAULT 'sent',   -- sent / failed
    error_message   TEXT,
    created_at      TIMESTAMPTZ DEFAULT NOW()
);

-- ============================================================
-- GPU 監控快照（選用，從 DCGM 定期寫入）
-- ============================================================

CREATE TABLE gpu_metrics (
    id              SERIAL PRIMARY KEY,
    host_id         INTEGER NOT NULL,             -- OpenNebula Host ID
    gpu_index       INTEGER DEFAULT 0,
    utilization     INTEGER,                      -- GPU 使用率 %
    temperature     INTEGER,                      -- 溫度 °C
    memory_used     INTEGER,                      -- 已用顯存 MB
    memory_total    INTEGER,                      -- 總顯存 MB
    power_usage     INTEGER,                      -- 功耗 W
    recorded_at     TIMESTAMPTZ DEFAULT NOW()
);

-- 索引
CREATE INDEX idx_applications_status ON applications(status);
CREATE INDEX idx_schedules_time ON schedules(start_time, end_time);
CREATE INDEX idx_schedules_user ON schedules(one_user_id);
CREATE INDEX idx_audit_logs_time ON audit_logs(created_at);
CREATE INDEX idx_audit_logs_user ON audit_logs(user_id);
CREATE INDEX idx_login_history_user ON login_history(user_id);
CREATE INDEX idx_gpu_metrics_host ON gpu_metrics(host_id, recorded_at);
```

---

## 微服務 API 設計

### 自建業務 API（存自己的 PostgreSQL）

```
POST   /api/applications              建立申請單
GET    /api/applications              查詢申請單列表（管理員）
GET    /api/applications/:id          查詢單筆申請
PUT    /api/applications/:id/approve  審核通過（自動建帳 + 建 VM + 發 Email）
PUT    /api/applications/:id/reject   審核拒絕（發拒絕 Email）

GET    /api/schedules                 查詢排程列表
POST   /api/schedules                 建立排程（寫 DB + 呼叫 vm.schedadd）
PUT    /api/schedules/:id             更新排程
DELETE /api/schedules/:id             刪除排程

POST   /api/vip/preempt              VIP 插隊（stop 佔用者 + resume VIP）
POST   /api/vip/restore              VIP 結束（stop VIP + resume 原本的）
GET    /api/vip/history               插隊歷史紀錄

GET    /api/audit                     查詢審計日誌
GET    /api/dashboard/gpu-overview    GPU 資源總覽（聚合 Host PCI 資訊）
GET    /api/dashboard/stats           平台統計（VM 數/使用者數/排隊數）
```

### 代理轉發 API（轉發到 FireEdge，不存 DB）

```
所有 /api/one/* 請求 → 轉發到 http://localhost:2616/fireedge/api/*

例如：
GET  /api/one/vm/info/0        → GET  http://localhost:2616/fireedge/api/vm/info/0
PUT  /api/one/vm/action/0      → PUT  http://localhost:2616/fireedge/api/vm/action/0
GET  /api/one/hostpool/info    → GET  http://localhost:2616/fireedge/api/hostpool/info
POST /api/one/auth/            → POST http://localhost:2616/fireedge/api/auth/

轉發時同時做：
1. 轉發 Cookie（維持 FireEdge session）
2. 記錄到 audit_logs 表（誰在什麼時間做了什麼）
```

---

## 資料流範例

### 學生申請 → 審核通過 → 拿到 VM

```
學生                    你的微服務                  FireEdge        oned
  │                        │                          │              │
  │ POST /api/applications │                          │              │
  │ {name, email, 學號}    │                          │              │
  ├───────────────────────►│                          │              │
  │                        │ 存入 PostgreSQL           │              │
  │                        │ 發 Email 通知管理者       │              │
  │                        │                          │              │
  │                     管理者                         │              │
  │                        │                          │              │
  │ PUT /applications/1/approve                       │              │
  │ {quota, template_id}   │                          │              │
  ├───────────────────────►│                          │              │
  │                        │── POST /one/user/allocate ──►           │
  │                        │◄── 回傳 user_id ──────────              │
  │                        │                          │              │
  │                        │── PUT /one/user/quota/5 ─────►          │
  │                        │◄── OK ───────────────────               │
  │                        │                          │              │
  │                        │── PUT /one/template/instantiate/1 ──►   │
  │                        │◄── 回傳 vm_id ───────────               │
  │                        │                          │              │
  │                        │ 更新 PostgreSQL           │              │
  │                        │ (application.status=approved)           │
  │                        │ (review.one_user_id=5, one_vm_id=10)   │
  │                        │                          │              │
  │                        │ 發 Email 給學生           │              │
  │                        │ (含金鑰 + 登入連結)       │              │
  │◄───────────────────────│                          │              │
  │                        │                          │              │
  │ 學生登入 + 啟動 VM     │                          │              │
  │ PUT /api/one/vm/action/10                         │              │
  │ {action: "resume"}     │                          │              │
  ├───────────────────────►│── PUT /vm/action/10 ────────►           │
  │                        │◄── OK ───────────────────               │
  │                        │ 記錄 audit_log            │              │
  │◄───────────────────────│                          │              │
```

### VIP 緊急插隊

```
管理者                  你的微服務                  FireEdge        oned
  │                        │                          │              │
  │ POST /api/vip/preempt  │                          │              │
  │ {vip_vm_id: 20,        │                          │              │
  │  target_host_id: 0}    │                          │              │
  ├───────────────────────►│                          │              │
  │                        │                          │              │
  │                        │── GET /one/host/info/0 ──────►          │
  │                        │◄── HOST_SHARE.PCI_DEVICES               │
  │                        │    (找到 GPU 被 VM#10 佔用)              │
  │                        │                          │              │
  │                        │── PUT /one/vm/action/10 ─────►          │
  │                        │   {action: "stop"}       │              │
  │                        │◄── OK（VM#10 暫停）───────              │
  │                        │                          │              │
  │                        │── PUT /one/vm/action/20 ─────►          │
  │                        │   {action: "resume"}     │              │
  │                        │◄── OK（VIP VM#20 啟動）──               │
  │                        │                          │              │
  │                        │ 寫入 PostgreSQL            │              │
  │                        │ vip_preemptions 表         │              │
  │                        │ 記錄 audit_log            │              │
  │                        │ 發 Email 通知被暫停的學生  │              │
  │◄───────────────────────│                          │              │
```

---

## 技術棧總覽

| 層級 | 技術 | 說明 |
|------|------|------|
| **前端** | React + Vite | 新開發的 GPU 算力平台 UI |
| | React Router | 頁面路由 |
| | TanStack Query 或 RTK Query | API 狀態管理 |
| | FullCalendar | 行事曆元件 |
| | Tailwind CSS 或 MUI | UI 框架 |
| **微服務** | Express.js | API 服務 |
| | node-postgres (pg) | PostgreSQL 連線 |
| | Prisma 或 Knex.js | ORM / Query Builder |
| | nodemailer | Email 發送 |
| | http-proxy-middleware | 轉發 FireEdge API |
| | jsonwebtoken | JWT 認證 |
| **資料庫** | PostgreSQL | 業務資料 |
| **不動的部分** | FireEdge (Express.js) | OpenNebula Web API |
| | oned (C++) | OpenNebula 核心 |
| | KVM | 虛擬化引擎 |
| | DCGM | GPU 監控 |

---

## 開發優先順序

### 第一階段：最小可用版本（MVP）

```
目標：學生能申請帳號 → 管理員審核 → 學生拿到 GPU VM

需完成：
1. Express 微服務骨架 + PostgreSQL 連線
2. 代理轉發 /api/one/* → FireEdge
3. 申請審核 API + 審核後自動建帳串接
4. React 前端：登入頁 + 申請表單 + VM 列表 + VM 操作
5. 管理員頁面：待審核列表 + 通過/拒絕
```

### 第二階段：核心功能

```
目標：管理員能排程 + VIP 能插隊

需完成：
1. 行事曆排程 API + 前端 FullCalendar
2. VIP 插隊 API + 一鍵插隊按鈕
3. GPU 資源總覽看板
4. Email 通知系統
5. 操作審計日誌
```

### 第三階段：進階功能

```
目標：完整的營運管理

需完成：
1. GPU 監控整合（DCGM → PostgreSQL → 前端圖表）
2. 使用時數統計 + 計費（Showback 整合或自建）
3. 活動紀錄查詢
4. 自訂儀表板
```
